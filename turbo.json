{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "dist-api/**"]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "passThroughEnv": [
        "REDIS_TEST_URL",
        "SKIP_REDIS_INTEGRATION",
        "DOCKER_HOST",
        "TESTCONTAINERS_*"
      ]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "build-storybook": {
      "dependsOn": ["^build", "build", "@nthstock/apiClient#build"],
      "outputs": ["storybook-static/**"]
    }
  }
}
