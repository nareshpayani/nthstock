# Architecture decision records

One file per decision: `NNNN-short-title.md` with Status, Date, Context, Decision, Consequences.
A decision is changed by a new ADR that supersedes the old one, never by editing history.
