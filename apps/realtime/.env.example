# apps/realtime local settings. Copy to .env if you need to change them; no secrets belong here.
PORT=8081
HOST=0.0.0.0
# Redis carrying ticks from apps/api (npm run infra:up starts it on this address).
REDIS_URL=redis://127.0.0.1:6379
