# apps/api local settings. Copy to .env if you need to change them; no secrets belong here.
PORT=4000
HOST=0.0.0.0
# Let the mock market tick outside NSE hours (demos, e2e). true or false.
MOCK_MARKET_ALWAYS_OPEN=false
